<!DOCTYPE html>
<html>
<head>
<style>
table {
  font-family: arial, sans-serif;
  border-collapse: collapse;
  width: 100%;
}

td, th {
  border: 1px solid #dddddd;
  text-align: left;
  padding: 8px;
}

tr:nth-child(even) {
  background-color: #dddddd;
}
</style>
</head>
<body>

<h2>category page</h2>
<table>
    <% cate.forEach((item,index)=> {%>
   
     <tr>
       <th><%=index+1%></th>
       <th><%=item.name%></th>
       <th><%=item.status%></th>
       <th><%=item.id%></th>
       <td>
           <a href="/update_category/<%=item.id%>" class="btn btn-info">Sua</a>
           <a href="/delete_category/<%=item.id%>" class="btn btn-danger">Xóa</a>
       </td>
     </tr>
   <% }) %>
     
     
   </table>`
   <br>
   <a href="/add_category"><button type="button">add new</button></a>

</body>
</html>
